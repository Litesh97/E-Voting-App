<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rajat.voting_app">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_web"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin_signUp" />
        <activity android:name=".voter_login" />
        <activity android:name=".Otp_verification" />
        <activity android:name=".HostElection1" />
        <activity android:name=".Host_election_confirm" />
        <activity android:name=".Vote_enter_election_id" />
        <activity android:name=".Actual_election_page" />
        <activity android:name=".Vote_confirm" />
        <activity android:name=".result_display"/>
        <activity android:name=".Manage_hosted_elections"></activity>
    </application>

</manifest>